name: Apply Sealed Product Scout

on:
  push:
    branches: [main]
    paths:
      - apply-sealed-product-scout.py
      - .github/workflows/apply-sealed-product-scout.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply Sealed Product Scout migration
        run: python apply-sealed-product-scout.py
      - name: Check syntax
        run: node --check sealed-product-scout.js
      - name: Run regression suite
        run: |
          node collection-intelligence.test.cjs
          for f in tests/*.test.cjs; do
            echo "Running $f"
            node "$f"
          done
      - name: Commit changes
        run: |
          git config user.name "HOF Card Scout Automation"
          git config user.email "actions@users.noreply.github.com"
          git add index.html tests/*.test.cjs
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Launch Sealed Product Scout first gate"
          git push
