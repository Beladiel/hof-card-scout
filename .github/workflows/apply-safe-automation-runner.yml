name: Apply safe automation runner

on:
  push:
    branches: [main]
    paths:
      - apply_safe_automation_runner.py
      - automation-runner-ui.js
      - tests/automation-runner.test.cjs
      - tests/automation-budget.test.cjs
      - .github/workflows/apply-safe-automation-runner.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply safe runner changes
        run: python apply_safe_automation_runner.py
      - name: Check Worker syntax
        run: node --check src/index.js
      - name: Run regression suite
        run: |
          node collection-intelligence.test.cjs
          for f in tests/*.test.cjs; do
            echo "Running $f"
            node "$f"
          done
      - name: Commit changes
        run: |
          git config user.name "HOF Card Scout Automation"
          git config user.email "actions@users.noreply.github.com"
          git add index.html automation-budget.js src/index.js tests/*.test.cjs
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Add one-search automation runner"
          git push
